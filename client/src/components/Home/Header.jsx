import React from 'react';
import Logo from "../Logo";
import Nav from "../Nav";

const Header = () => {
    return (
        <header>
            <Logo />
            <Nav />
          </header>
    );
};

export default Header;